/**
 * MovieFilter — Bộ lọc phim
 *
 * Dùng: useState cho filter state đơn giản
 * Filters: thể loại (select), trạng thái (tabs), search text (input)
 * Callback: onChange → truyền lên parent (MoviesPage)
 */
