/**
 * MovieList — Danh sách phim
 *
 * Dùng: useMemo để filter/sort danh sách phim client-side
 * Props: { movies: Movie[], selectedGenre, sortBy }
 * Render: grid <MovieCard /> responsive (1 col mobile, 2-3 tablet, 4 desktop)
 */
